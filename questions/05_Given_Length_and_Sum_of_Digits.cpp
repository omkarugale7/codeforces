#include <bits/stdc++.h>

using namespace std;
typedef long long int ll;
int main()
{
    int n, m;
    cin >> n >> m;
    if (m <= 1 || m >=( 9 * n))
        cout << "NO" << endl;
    else
    {
        int min,max;
        
    }

    return 0;
}